// Just like how objects have key and are declared like, 'obj[key]'
// Strings have index for each characters targeted by, 'str[0]'
// They ALLWAYS Start from '0'

let str = 'Hello'

console.log(`The 5th Letter of ${str} is ${str[4]}`);