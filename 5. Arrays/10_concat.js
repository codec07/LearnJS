// used to join multiple arrays to one

let fruits = ['apple', 'banana', 'mango'];
let vegetables = ['spinach', 'carrots', 'beetroot'];

let combined = fruits.concat(vegetables);
console.log(combined);