// Used to get a part of array (similar to strings)
// Format: arr.slice(startIndex, endIndex);

let fruits = ['apple', 'banana', 'mango', 'watermelon', 'papaya', 'orange'];
console.log(fruits);

let newFruits = fruits.slice(2, 5);
console.log(newFruits);