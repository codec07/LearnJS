/*
In JS function also have parameters (arguments) in them.

FORMAT:

function sayMsg(msg) {
    console.log(msg);
}

sayMsg("Hello! World")

These parameters can be used anywhere in the whole program and also make process easy.
*/

// EXAMPLE 1 - PRINTING SOMETHING
function sayMsg(msg) {
    console.log(msg);
}

sayMsg("I Printed something using functions!!");

// EXAMPLE 2 - ADDING TWO NUMBERS

function sum(num1, num2) {
    console.log(num1 + num2);
}

sum(123, 123); // PRINT - 246